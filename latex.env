# LaTeX tool configuration for this project
# Format: KEY=VALUE (one per line). Lines starting with # are comments.
# The Python build script will read this file to locate tools cross-platform.

# REQUIRED: Path or command for pdflatex
# Example (Windows MiKTeX, absolute path):
PDFLATEX=C:\Users\xinin\AppData\Local\Programs\MiKTeX\miktex\bin\x64\pdflatex.exe
# Example (macOS or Linux on PATH):
# PDFLATEX=pdflatex

# REQUIRED: Path or command for bibtex
# Example (Windows MiKTeX, absolute path):
BIBTEX=C:\Users\xinin\AppData\Local\Programs\MiKTeX\miktex\bin\x64\bibtex.exe
# Example (macOS/Linux on PATH):
# BIBTEX=bibtex

# Optional: interaction mode for pdflatex (default: nonstopmode)
# INTERACTION=nonstopmode

# Optional: pass -file-line-error to pdflatex (default: true)
# FILE_LINE_ERROR=true

# Optional: number of final pdflatex passes after bibtex (default: 2)
# PDFLATEX_FINAL_PASSES=2
